id: list-orchestration-folder
namespace: dev

description: |
  Workflow de test pour vérifier l'accès aux fichiers montés dans /mnt/orchestration/data

tasks:
  - id: list-files
    type: io.kestra.plugin.fs.LocalFiles.List
    description: Liste les fichiers dans /mnt/orchestration/data
    path: /mnt/orchestration/data
